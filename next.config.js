/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
}

module.exports = {
  images: {
    domains: [
      'images.unsplash.com',
      'www.stutalk.in',
      'mdbootstrap.com',
      'assets.coingecko.com',
      'icons8.com',
      'cdn.tuk.dev',
    ]
  }
}
